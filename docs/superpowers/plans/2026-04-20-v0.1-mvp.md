# sush.sh v0.1 MVP 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标（Goal）：** 从骨架代码出发，完整实现 sush.sh v0.1 MVP —— 终端 SSH + SFTP 统一管理工具，支持 ssh config 自动导入、模糊搜索、SSH 接管模式、SSH↔SFTP 无缝切换、文件上传下载及进度显示。

**架构（Architecture）：** 严格遵循 `ARCHITECTURE.md` 已锁定的方案：Rust + tokio + ratatui + russh + russh-sftp。状态机以 `AppMode` 枚举驱动三种模式（Main / Ssh / Sftp），SSH 与 SFTP 共享同一 TCP 连接（不同 channel），SSH 使用 I/O 接管 + 前缀键 `Ctrl-Space`（raw mode 下字节 `0x00`）切换模式。

**技术栈（Tech Stack）：** Rust 1.95 (edition 2024) · tokio 1.52 · ratatui 0.30 · crossterm 0.29 · russh 0.60 · russh-sftp 2.1 · nucleo 0.5 · ssh2-config 0.7 · serde + toml · anyhow · dirs 6.0 · uuid 1.23。

**参考文档：**
- 功能需求：`REQUIREMENTS.md`
- 架构设计：`ARCHITECTURE.md`
- 线路图：`ROADMAP.md`
- 任务拆分来源：`TASKS.md`

**执行策略：**
- 分 5 个阶段按依赖顺序执行：基础设施 → TUI 框架 → SSH 连接 → SFTP → 收尾
- 每个任务结束后单独 commit
- TDD 优先：对纯逻辑模块（config / utils / ssh_config 解析）先写单元测试
- 对涉及真实 SSH 服务器 / TUI 渲染的任务，采用集成手测 + 小型单元测试混合方式
- 所有代码注释使用中文

## 进度追踪

| 任务 | 状态 | Commit | 备注 |
|------|------|--------|------|
| Task 0 基线校验 | ✅ | bd96373 | tempfile dev-dep 添加 |
| Task 1 SSH Config 解析 | ✅ | bd96373 | 3 单测通过，api 按实际调整 |
| Task 2 配置持久化 + 增量同步 | ✅ | bd96373 | 4 单测通过 |
| Task 3 模糊搜索封装 | ✅ | bd96373 | 4 单测通过，nucleo 返回 u16 |
| Task 4 TUI 事件循环 | ✅ | 735f2ef | crossterm event-stream feature |
| Task 5 主界面渲染 | ✅ | 735f2ef | SearchInput/HostList/StatusBar widget |
| Task 6 搜索交互 + ssh config 导入 | ✅ | 735f2ef | 13 单测全部通过 |
| Task 7 SSH 认证流程 | 🔄 进行中 | - | russh 0.60 不需要 async-trait |
| Task 8 密码输入弹窗 | ⬜ | - | |
| Task 9 SSH 接管模式 | ⬜ | - | |
| Task 10 前缀键 + resize | ⬜ | - | |
| Task 11 SFTP 客户端封装 | ⬜ | - | |
| Task 12 SFTP 文件浏览器 UI | ⬜ | - | |
| Task 13 文件上传/下载 + 进度 | ⬜ | - | |
| Task 14 双 Ctrl-C + 模式切换完善 | ⬜ | - | |
| Task 15 端到端集成测试 | ⬜ | - | |

---

## 阶段 0：依赖与基建校验

在进入 Phase 1 之前，先确保开发环境可用。

### Task 0: 基线校验

**Files:**
- Modify: `Cargo.toml`（仅当新增 dev-dependencies 时）

- [ ] **Step 1: 确认 toolchain**

运行：
```bash
cargo --version
rustc --version
```
预期输出：`rustc 1.95.x (edition 2024)`。如果版本不符，`mise install` 安装锁定版本。

- [ ] **Step 2: 基线 check**

运行：
```bash
cargo check
```
预期：通过，可能有未使用代码的 warning。

- [ ] **Step 3: 基线 clippy**

运行：
```bash
cargo clippy -- -D warnings
```
预期：通过（骨架代码应无 warning）。若有 warning，先修复再进入后续任务。

- [ ] **Step 4: 新增测试辅助依赖**

编辑 `Cargo.toml`，在 `[dependencies]` 段落末尾之后追加：

```toml
[dev-dependencies]
tempfile = "3.10"
```

- [ ] **Step 5: 验证编译**

运行：
```bash
cargo check --tests
```
预期：通过。

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "chore: 添加 tempfile 作为测试辅助依赖"
```

---

## 阶段 1：基础设施（T1–T3，无 UI 依赖）

### Task 1: SSH Config 解析（T1）

**Files:**
- Modify: `src/config/ssh_config.rs`
- Test: `src/config/ssh_config.rs`（内嵌 `#[cfg(test)]`）

**目标：** 从 `~/.ssh/config` 读取文件，解析为 `Vec<Host>`，跳过通配符 Host，支持多 `IdentityFile`。

**关键约束：**
- 字段支持：`Host`、`HostName`、`User`、`Port`、`IdentityFile`、`ProxyJump`
- 通配符（含 `*`、`?`、`!`）Host 跳过
- 导入的主机 `source = HostSource::SshConfig`
- `id` 生成规则：使用 `alias`（host 名）本身即可，保证增量同步时可匹配
- 返回 `(Vec<Host>, content_hash)` 方便 Task 2 做增量对比

- [ ] **Step 1: 写失败单元测试（基础解析）**

替换 `src/config/ssh_config.rs` 末尾的 `compute_hash` 后追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::io::Write;
    use tempfile::NamedTempFile;

    fn write_config(content: &str) -> NamedTempFile {
        let mut f = NamedTempFile::new().unwrap();
        f.write_all(content.as_bytes()).unwrap();
        f
    }

    #[test]
    fn parse_single_host() {
        let f = write_config(
            "Host prod-web\n  HostName 10.0.0.1\n  User deploy\n  Port 2222\n",
        );
        let (hosts, _) = parse_ssh_config(f.path()).unwrap();
        assert_eq!(hosts.len(), 1);
        let h = &hosts[0];
        assert_eq!(h.alias, "prod-web");
        assert_eq!(h.hostname, "10.0.0.1");
        assert_eq!(h.user, "deploy");
        assert_eq!(h.port, 2222);
        assert!(matches!(h.source, HostSource::SshConfig));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib parse_single_host
```
预期：`error[E0425]: cannot find function parse_ssh_config`。

- [ ] **Step 3: 实现 parse_ssh_config（最小可运行）**

将 `src/config/ssh_config.rs` 完整替换为：

```rust
use std::collections::hash_map::DefaultHasher;
use std::fs;
use std::hash::{Hash, Hasher};
use std::path::{Path, PathBuf};

use anyhow::{Context, Result};
use ssh2_config::{ParseRule, SshConfig};

use super::host::{Host, HostSource};

pub fn import_ssh_config() -> Result<(Vec<Host>, String)> {
    let path = dirs::home_dir().unwrap_or_default().join(".ssh/config");
    if !path.exists() {
        return Ok((Vec::new(), String::new()));
    }
    parse_ssh_config(&path)
}

pub fn parse_ssh_config(path: &Path) -> Result<(Vec<Host>, String)> {
    let content = fs::read_to_string(path)
        .with_context(|| format!("读取 ssh config 失败: {}", path.display()))?;
    let hash = compute_hash(&content);

    let mut reader = std::io::BufReader::new(content.as_bytes());
    let config = SshConfig::default()
        .parse(&mut reader, ParseRule::ALLOW_UNKNOWN_FIELDS)
        .context("解析 ssh config 失败")?;

    let hosts = config
        .get_hosts()
        .iter()
        .filter_map(|h| host_from_entry(h))
        .collect();

    Ok((hosts, hash))
}

fn host_from_entry(entry: &ssh2_config::Host) -> Option<Host> {
    let alias = entry
        .pattern
        .iter()
        .find_map(|p| {
            let s = p.pattern.as_str();
            if is_wildcard(s) { None } else { Some(s.to_string()) }
        })?;

    let params = &entry.params;
    let hostname = params.host_name.clone().unwrap_or_else(|| alias.clone());
    let user = params.user.clone().unwrap_or_default();
    let port = params.port.unwrap_or(22);
    let identity_files: Vec<PathBuf> = params
        .identity_file
        .clone()
        .unwrap_or_default();
    let proxy_jump = None; // ssh2-config 0.7 无原生字段；v0.1 暂不处理 ProxyJump

    Some(Host {
        id: alias.clone(),
        alias,
        hostname,
        port,
        user,
        identity_files,
        proxy_jump,
        tags: Vec::new(),
        description: String::new(),
        source: HostSource::SshConfig,
    })
}

fn is_wildcard(pattern: &str) -> bool {
    pattern.contains('*') || pattern.contains('?') || pattern.starts_with('!')
}

fn compute_hash(content: &str) -> String {
    let mut hasher = DefaultHasher::new();
    content.hash(&mut hasher);
    format!("{:x}", hasher.finish())
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::io::Write;
    use tempfile::NamedTempFile;

    fn write_config(content: &str) -> NamedTempFile {
        let mut f = NamedTempFile::new().unwrap();
        f.write_all(content.as_bytes()).unwrap();
        f
    }

    #[test]
    fn parse_single_host() {
        let f = write_config(
            "Host prod-web\n  HostName 10.0.0.1\n  User deploy\n  Port 2222\n",
        );
        let (hosts, _) = parse_ssh_config(f.path()).unwrap();
        assert_eq!(hosts.len(), 1);
        let h = &hosts[0];
        assert_eq!(h.alias, "prod-web");
        assert_eq!(h.hostname, "10.0.0.1");
        assert_eq!(h.user, "deploy");
        assert_eq!(h.port, 2222);
        assert!(matches!(h.source, HostSource::SshConfig));
    }
}
```

> **注意：** `ssh2-config` 的 API 名称可能随版本微调，若编译失败请先 `cargo doc --open -p ssh2-config` 确认实际签名（`pattern`/`params`/`host_name` 等字段）。如发现 `ProxyJump` 字段存在，再补上 `proxy_jump = params.proxy_jump.clone();`。

- [ ] **Step 4: 运行测试确认通过**

```bash
cargo test --lib parse_single_host
```
预期：PASS。

- [ ] **Step 5: 补充通配符跳过测试**

在 `mod tests` 中追加：

```rust
#[test]
fn skip_wildcard_host() {
    let f = write_config(
        "Host *\n  User root\n\nHost prod-*\n  User deploy\n\nHost real-host\n  HostName 1.2.3.4\n",
    );
    let (hosts, _) = parse_ssh_config(f.path()).unwrap();
    assert_eq!(hosts.len(), 1);
    assert_eq!(hosts[0].alias, "real-host");
}
```

- [ ] **Step 6: 补充多 IdentityFile 测试**

追加：

```rust
#[test]
fn multiple_identity_files() {
    let f = write_config(
        "Host multi\n  HostName x\n  IdentityFile ~/.ssh/id_a\n  IdentityFile ~/.ssh/id_b\n",
    );
    let (hosts, _) = parse_ssh_config(f.path()).unwrap();
    assert_eq!(hosts.len(), 1);
    assert_eq!(hosts[0].identity_files.len(), 2);
}
```

- [ ] **Step 7: 运行全部测试**

```bash
cargo test --lib ssh_config::tests
```
预期：3 passed。若 `multiple_identity_files` 失败，说明 `ssh2-config` 对同名字段取覆盖语义而非累加；此时改为使用底层文本解析（自行扫描以 `IdentityFile` 开头的行）。

- [ ] **Step 8: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```
两者都需通过。

- [ ] **Step 9: Commit**

```bash
git add src/config/ssh_config.rs Cargo.toml Cargo.lock
git commit -m "feat(config): 实现 ssh config 解析（支持多 IdentityFile，跳过通配符）"
```

---

### Task 2: 配置持久化 + 增量同步（T2）

**Files:**
- Modify: `src/config/store.rs`
- Test: `src/config/store.rs`（内嵌 `#[cfg(test)]`）

**目标：** 增加 `merge_ssh_config_hosts` 和 `load_or_init` 函数；实现：
- hash 未变 → 不重导
- hash 变化 → 增量同步（新增/更新，不删除 `Manual` 来源主机）
- 支持自定义配置目录（通过参数注入，便于测试）

- [ ] **Step 1: 写失败测试（往返一致）**

在 `src/config/store.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::config::host::{Host, HostSource};
    use tempfile::TempDir;

    fn sample_host(id: &str, source: HostSource) -> Host {
        Host {
            id: id.into(),
            alias: id.into(),
            hostname: "1.1.1.1".into(),
            port: 22,
            user: "u".into(),
            identity_files: vec![],
            proxy_jump: None,
            tags: vec![],
            description: String::new(),
            source,
        }
    }

    #[test]
    fn save_load_roundtrip() {
        let dir = TempDir::new().unwrap();
        let path = dir.path().join("hosts.toml");
        let hosts = vec![sample_host("a", HostSource::Manual)];
        save_to(&path, &hosts, "hash1").unwrap();
        let (loaded, hash) = load_from(&path).unwrap();
        assert_eq!(loaded.len(), 1);
        assert_eq!(loaded[0].id, "a");
        assert_eq!(hash, "hash1");
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib save_load_roundtrip
```
预期：`cannot find function save_to/load_from`。

- [ ] **Step 3: 重写 store.rs**

将 `src/config/store.rs` 完整替换为：

```rust
use std::fs;
use std::path::{Path, PathBuf};

use anyhow::{Context, Result};
use serde::{Deserialize, Serialize};

use super::host::{Host, HostSource};

#[derive(Debug, Serialize, Deserialize, Default)]
pub struct HostStore {
    #[serde(default)]
    pub metadata: Metadata,
    #[serde(default)]
    pub hosts: Vec<Host>,
}

#[derive(Debug, Serialize, Deserialize, Default)]
pub struct Metadata {
    #[serde(default)]
    pub ssh_config_hash: String,
}

pub fn config_dir() -> PathBuf {
    dirs::config_dir()
        .unwrap_or_else(|| PathBuf::from("~/.config"))
        .join("sushi")
}

pub fn config_path() -> PathBuf {
    config_dir().join("hosts.toml")
}

/// 加载主机列表（路径由内部决定）。文件不存在时返回空列表。
pub fn load_hosts() -> Result<Vec<Host>> {
    let (hosts, _) = load_from(&config_path())?;
    Ok(hosts)
}

/// 加载并返回 (hosts, ssh_config_hash)。
pub fn load_from(path: &Path) -> Result<(Vec<Host>, String)> {
    if !path.exists() {
        return Ok((Vec::new(), String::new()));
    }
    let content = fs::read_to_string(path)
        .with_context(|| format!("读取配置失败: {}", path.display()))?;
    let store: HostStore = toml::from_str(&content).context("解析配置失败")?;
    Ok((store.hosts, store.metadata.ssh_config_hash))
}

pub fn save_hosts(hosts: &[Host], ssh_config_hash: &str) -> Result<()> {
    save_to(&config_path(), hosts, ssh_config_hash)
}

pub fn save_to(path: &Path, hosts: &[Host], ssh_config_hash: &str) -> Result<()> {
    if let Some(dir) = path.parent() {
        fs::create_dir_all(dir)?;
    }
    let store = HostStore {
        metadata: Metadata {
            ssh_config_hash: ssh_config_hash.to_string(),
        },
        hosts: hosts.to_vec(),
    };
    let content = toml::to_string_pretty(&store)?;
    fs::write(path, content)?;
    Ok(())
}

/// 增量合并：从 ssh config 新导入的主机并入已有主机列表。
/// 规则：
/// - 与已有主机 `id` 相同且 source = SshConfig → 更新字段
/// - id 不存在 → 新增
/// - Manual 主机永不被动过
/// - ssh config 中删除的 Host → 不从结果中移除
pub fn merge_ssh_config_hosts(existing: Vec<Host>, imported: Vec<Host>) -> Vec<Host> {
    let mut result = existing;
    for new_h in imported {
        if let Some(slot) = result
            .iter_mut()
            .find(|h| h.id == new_h.id && matches!(h.source, HostSource::SshConfig))
        {
            // 保留用户自定义字段（tags, description）
            let tags = std::mem::take(&mut slot.tags);
            let description = std::mem::take(&mut slot.description);
            *slot = Host {
                tags,
                description,
                ..new_h
            };
        } else if !result.iter().any(|h| h.id == new_h.id) {
            result.push(new_h);
        }
    }
    result
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::config::host::{Host, HostSource};
    use tempfile::TempDir;

    fn sample_host(id: &str, source: HostSource) -> Host {
        Host {
            id: id.into(),
            alias: id.into(),
            hostname: "1.1.1.1".into(),
            port: 22,
            user: "u".into(),
            identity_files: vec![],
            proxy_jump: None,
            tags: vec![],
            description: String::new(),
            source,
        }
    }

    #[test]
    fn save_load_roundtrip() {
        let dir = TempDir::new().unwrap();
        let path = dir.path().join("hosts.toml");
        let hosts = vec![sample_host("a", HostSource::Manual)];
        save_to(&path, &hosts, "hash1").unwrap();
        let (loaded, hash) = load_from(&path).unwrap();
        assert_eq!(loaded.len(), 1);
        assert_eq!(loaded[0].id, "a");
        assert_eq!(hash, "hash1");
    }

    #[test]
    fn merge_adds_new_ssh_config_hosts() {
        let existing = vec![sample_host("m1", HostSource::Manual)];
        let imported = vec![sample_host("s1", HostSource::SshConfig)];
        let merged = merge_ssh_config_hosts(existing, imported);
        assert_eq!(merged.len(), 2);
    }

    #[test]
    fn merge_updates_existing_ssh_config_host_but_keeps_tags() {
        let mut old = sample_host("s1", HostSource::SshConfig);
        old.tags = vec!["prod".into()];
        old.hostname = "old".into();
        let mut incoming = sample_host("s1", HostSource::SshConfig);
        incoming.hostname = "new".into();

        let merged = merge_ssh_config_hosts(vec![old], vec![incoming]);
        assert_eq!(merged.len(), 1);
        assert_eq!(merged[0].hostname, "new");
        assert_eq!(merged[0].tags, vec!["prod".to_string()]);
    }

    #[test]
    fn merge_never_touches_manual_hosts() {
        let existing = vec![sample_host("m1", HostSource::Manual)];
        // ssh config 中没有 m1 → m1 保留不动
        let merged = merge_ssh_config_hosts(existing.clone(), vec![]);
        assert_eq!(merged.len(), 1);
        assert_eq!(merged[0].id, "m1");
    }
}
```

- [ ] **Step 4: 运行全部测试**

```bash
cargo test --lib store::tests
```
预期：4 passed。

- [ ] **Step 5: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 6: Commit**

```bash
git add src/config/store.rs
git commit -m "feat(config): 配置持久化 + ssh config 增量合并"
```

---

### Task 3: 模糊搜索封装（T3）

**Files:**
- Modify: `src/utils/fuzzy.rs`
- Test: `src/utils/fuzzy.rs`

**目标：** 封装 `nucleo` 的同步匹配 API，提供 `search(query, hosts) -> Vec<usize>`。匹配维度：alias、hostname、user、tags、description。空 query 返回全部。

- [ ] **Step 1: 写失败测试**

将 `src/utils/fuzzy.rs` 完整替换为：

```rust
use crate::config::host::Host;

/// 对 hosts 做多维模糊搜索，返回命中的原始索引（按匹配分数降序）。
/// query 为空时返回 0..hosts.len() 的顺序索引。
pub fn search(query: &str, hosts: &[Host]) -> Vec<usize> {
    if query.trim().is_empty() {
        return (0..hosts.len()).collect();
    }

    use nucleo::{Config, Matcher, Utf32Str};

    let mut matcher = Matcher::new(Config::DEFAULT);
    let needle_owned = query.to_lowercase();
    let mut needle_buf = Vec::new();
    let needle = Utf32Str::new(&needle_owned, &mut needle_buf);

    let mut scored: Vec<(usize, u32)> = hosts
        .iter()
        .enumerate()
        .filter_map(|(i, h)| {
            let haystack = build_haystack(h).to_lowercase();
            let mut haystack_buf = Vec::new();
            let hay = Utf32Str::new(&haystack, &mut haystack_buf);
            matcher.fuzzy_match(hay, needle).map(|s| (i, s))
        })
        .collect();

    scored.sort_by(|a, b| b.1.cmp(&a.1));
    scored.into_iter().map(|(i, _)| i).collect()
}

fn build_haystack(h: &Host) -> String {
    let mut s = String::new();
    s.push_str(&h.alias);
    s.push(' ');
    s.push_str(&h.hostname);
    s.push(' ');
    s.push_str(&h.user);
    s.push(' ');
    s.push_str(&h.tags.join(" "));
    s.push(' ');
    s.push_str(&h.description);
    s
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::config::host::{Host, HostSource};
    use std::path::PathBuf;

    fn make(id: &str, hostname: &str, user: &str, tags: &[&str]) -> Host {
        Host {
            id: id.into(),
            alias: id.into(),
            hostname: hostname.into(),
            port: 22,
            user: user.into(),
            identity_files: Vec::<PathBuf>::new(),
            proxy_jump: None,
            tags: tags.iter().map(|s| s.to_string()).collect(),
            description: String::new(),
            source: HostSource::Manual,
        }
    }

    #[test]
    fn empty_query_returns_all() {
        let hosts = vec![make("a", "x", "u", &[]), make("b", "y", "u", &[])];
        assert_eq!(search("", &hosts), vec![0, 1]);
    }

    #[test]
    fn matches_alias() {
        let hosts = vec![
            make("prod-web", "10.0.0.1", "deploy", &[]),
            make("dev-db", "10.0.0.2", "root", &[]),
        ];
        let result = search("web", &hosts);
        assert_eq!(result, vec![0]);
    }

    #[test]
    fn matches_tag() {
        let hosts = vec![
            make("a", "x", "u", &["web"]),
            make("b", "y", "u", &["db"]),
        ];
        let result = search("db", &hosts);
        assert!(result.contains(&1));
    }

    #[test]
    fn matches_hostname() {
        let hosts = vec![make("a", "example.com", "u", &[])];
        assert_eq!(search("example", &hosts), vec![0]);
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cargo test --lib fuzzy::tests
```
预期：4 passed。若 `nucleo::Matcher` API 与上文不一致，查阅 `cargo doc --open -p nucleo`。nucleo 0.5 核心函数为 `Matcher::fuzzy_match(hay, needle) -> Option<u32>`。

- [ ] **Step 3: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 4: Commit**

```bash
git add src/utils/fuzzy.rs
git commit -m "feat(utils): nucleo 封装，多维模糊搜索主机"
```

---

## 阶段 2：TUI 框架（T4–T6）

### Task 4: TUI 事件循环（T4）

**Files:**
- Modify: `src/tui/event.rs`
- Modify: `src/tui/mod.rs`
- Modify: `src/app.rs`

**目标：** 建立基本事件循环：进入 raw mode + alternate screen → 每帧轮询 crossterm 事件 → 把 key/resize 派发给 App → q 或 Ctrl-C 退出。

关键点：事件源用 `tokio::select!` 合并 —— v0.1 初期只有键盘 + resize，但结构需预留后续 `SshOutput` / `TransferProgress` 分支。

- [ ] **Step 1: 定义事件类型**

将 `src/tui/event.rs` 完整替换为：

```rust
use crossterm::event::{Event as CtEvent, EventStream, KeyEvent};
use futures::StreamExt;
use std::time::Duration;
use tokio::sync::mpsc;
use tokio::time;

/// App 层消费的统一事件。
#[derive(Debug)]
pub enum AppEvent {
    Key(KeyEvent),
    Resize(u16, u16),
    Tick,
    /// 来自异步子系统的透传（SSH 输出、传输进度等，后续 Task 填充）。
    External(ExternalEvent),
}

#[derive(Debug)]
pub enum ExternalEvent {
    // 占位：Task 9 之后补充 SshOutput(Vec<u8>)
    // 占位：Task 13 之后补充 TransferProgress(...)
}

pub struct EventBus {
    rx: mpsc::Receiver<AppEvent>,
}

impl EventBus {
    pub fn new() -> Self {
        let (tx, rx) = mpsc::channel(128);
        spawn_terminal_reader(tx.clone());
        Self { rx }
    }

    pub async fn next(&mut self) -> Option<AppEvent> {
        self.rx.recv().await
    }
}

fn spawn_terminal_reader(tx: mpsc::Sender<AppEvent>) {
    tokio::spawn(async move {
        let mut reader = EventStream::new();
        let mut ticker = time::interval(Duration::from_millis(250));
        loop {
            tokio::select! {
                _ = ticker.tick() => {
                    if tx.send(AppEvent::Tick).await.is_err() { break; }
                }
                Some(Ok(ev)) = reader.next() => {
                    let mapped = match ev {
                        CtEvent::Key(k) => Some(AppEvent::Key(k)),
                        CtEvent::Resize(w, h) => Some(AppEvent::Resize(w, h)),
                        _ => None,
                    };
                    if let Some(e) = mapped {
                        if tx.send(e).await.is_err() { break; }
                    }
                }
            }
        }
    });
}
```

**依赖说明：** `EventStream` 需要 `crossterm` 的 `event-stream` feature。同时 `futures` 需要显式声明。更新 `Cargo.toml` 的 `[dependencies]`：

```toml
crossterm = { version = "0.29", features = ["event-stream"] }
futures = "0.3"
```

- [ ] **Step 2: 改写 App::run 进入主循环**

替换 `src/app.rs` 为：

```rust
use anyhow::Result;
use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
use ratatui::Terminal;
use ratatui::backend::CrosstermBackend;
use std::io::{Stdout, stdout};

use crate::config::host::Host;
use crate::config::store;
use crate::tui::event::{AppEvent, EventBus};

pub enum AppMode {
    Main,
    Ssh,
    Sftp,
}

pub struct App {
    pub mode: AppMode,
    pub hosts: Vec<Host>,
    pub search_query: String,
    pub filtered_indices: Vec<usize>,
    pub selected_index: usize,
    pub should_quit: bool,
}

impl App {
    pub fn new() -> Result<Self> {
        let hosts = store::load_hosts()?;
        let filtered_indices = (0..hosts.len()).collect();
        Ok(Self {
            mode: AppMode::Main,
            hosts,
            search_query: String::new(),
            filtered_indices,
            selected_index: 0,
            should_quit: false,
        })
    }

    pub async fn run(&mut self) -> Result<()> {
        let mut terminal = setup_terminal()?;
        let mut bus = EventBus::new();

        let result = self.event_loop(&mut terminal, &mut bus).await;

        restore_terminal(&mut terminal)?;
        result
    }

    async fn event_loop(
        &mut self,
        terminal: &mut Terminal<CrosstermBackend<Stdout>>,
        bus: &mut EventBus,
    ) -> Result<()> {
        while !self.should_quit {
            terminal.draw(|f| {
                // Task 5 填充具体视图渲染
                let area = f.area();
                let block = ratatui::widgets::Block::bordered().title("sushi");
                f.render_widget(block, area);
            })?;

            if let Some(ev) = bus.next().await {
                self.handle_event(ev);
            }
        }
        Ok(())
    }

    fn handle_event(&mut self, ev: AppEvent) {
        match ev {
            AppEvent::Key(k) => self.handle_key(k),
            AppEvent::Resize(_, _) | AppEvent::Tick | AppEvent::External(_) => {}
        }
    }

    fn handle_key(&mut self, k: KeyEvent) {
        match (k.code, k.modifiers) {
            (KeyCode::Char('q'), KeyModifiers::NONE) => self.should_quit = true,
            (KeyCode::Char('c'), KeyModifiers::CONTROL) => self.should_quit = true,
            _ => {}
        }
    }
}

fn setup_terminal() -> Result<Terminal<CrosstermBackend<Stdout>>> {
    crossterm::terminal::enable_raw_mode()?;
    let mut out = stdout();
    crossterm::execute!(
        out,
        crossterm::terminal::EnterAlternateScreen,
        crossterm::event::EnableMouseCapture
    )?;
    Ok(Terminal::new(CrosstermBackend::new(out))?)
}

fn restore_terminal(
    terminal: &mut Terminal<CrosstermBackend<Stdout>>,
) -> Result<()> {
    crossterm::terminal::disable_raw_mode()?;
    crossterm::execute!(
        terminal.backend_mut(),
        crossterm::terminal::LeaveAlternateScreen,
        crossterm::event::DisableMouseCapture
    )?;
    terminal.show_cursor()?;
    Ok(())
}
```

- [ ] **Step 3: 验证 cargo check**

```bash
cargo check
```
预期：通过。若 ratatui API 有差异（`f.area()` vs `f.size()`），以 ratatui 0.30 文档为准。

- [ ] **Step 4: 手动冒烟测试**

```bash
cargo run
```
预期：
- 终端切换到备用缓冲区
- 显示空的 "sushi" 边框
- 按 `q` 或 `Ctrl-C` 正常退出并恢复终端

退出后终端应无残留（光标可见、无覆盖内容）。如果退出后终端损坏，执行 `reset` 恢复。

- [ ] **Step 5: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src/app.rs src/tui/event.rs
git commit -m "feat(tui): 事件循环骨架，支持 q/Ctrl-C 退出"
```

---

### Task 5: 主界面渲染（T5）

**Files:**
- Modify: `src/tui/views/main_view.rs`
- Modify: `src/tui/widgets/search_input.rs`
- Modify: `src/tui/widgets/host_list.rs`
- Modify: `src/tui/widgets/status_bar.rs`
- Modify: `src/app.rs`

**目标：** 渲染三段布局 —— 顶部搜索框、中间主机列表（有选中高亮、显示别名/IP/标签）、底部快捷键栏。上下键切换选中。

**布局参考（来自 REQUIREMENTS.md）：**
```
┌─ sushi ────────────────────────────────────────┐
│  > 搜索...  █                                   │
│  ┌──────────────────────────────────────────┐  │
│  │ ● prod-web-01   192.168.1.10      web   │  │
│  └──────────────────────────────────────────┘  │
│  Enter:SSH  F2:SFTP  F5:新建  ?:帮助  Q:退出    │
└────────────────────────────────────────────────┘
```

- [ ] **Step 1: 实现 search_input widget**

替换 `src/tui/widgets/search_input.rs`：

```rust
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
use ratatui::style::{Style, Stylize};
use ratatui::text::Line;
use ratatui::widgets::{Block, Paragraph, Widget};

pub struct SearchInput<'a> {
    pub query: &'a str,
    pub focused: bool,
}

impl<'a> Widget for SearchInput<'a> {
    fn render(self, area: Rect, buf: &mut Buffer) {
        let cursor = if self.focused { "█" } else { "" };
        let text = if self.query.is_empty() && !self.focused {
            "搜索...".to_string()
        } else {
            format!("{}{}", self.query, cursor)
        };
        let style = if self.query.is_empty() && !self.focused {
            Style::default().dim()
        } else {
            Style::default()
        };
        Paragraph::new(Line::from(text).style(style))
            .block(Block::bordered().title(" > "))
            .render(area, buf);
    }
}
```

- [ ] **Step 2: 实现 host_list widget**

替换 `src/tui/widgets/host_list.rs`：

```rust
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
use ratatui::style::{Color, Modifier, Style, Stylize};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, List, ListItem, ListState, StatefulWidget};

use crate::config::host::Host;

pub struct HostList<'a> {
    pub hosts: &'a [Host],
    pub indices: &'a [usize],
}

impl<'a> StatefulWidget for HostList<'a> {
    type State = ListState;

    fn render(self, area: Rect, buf: &mut Buffer, state: &mut Self::State) {
        let items: Vec<ListItem> = self
            .indices
            .iter()
            .map(|&i| {
                let h = &self.hosts[i];
                let tags = if h.tags.is_empty() {
                    String::new()
                } else {
                    h.tags.join(",")
                };
                ListItem::new(Line::from(vec![
                    Span::raw("  "),
                    Span::styled(
                        format!("{:<16}", h.alias),
                        Style::default().fg(Color::Cyan),
                    ),
                    Span::raw(format!("{:<20}", h.hostname)),
                    Span::styled(tags, Style::default().dim()),
                ]))
            })
            .collect();
        List::new(items)
            .block(Block::bordered().title(" 主机 "))
            .highlight_style(
                Style::default()
                    .add_modifier(Modifier::REVERSED)
                    .fg(Color::Yellow),
            )
            .highlight_symbol("● ")
            .render(area, buf, state);
    }
}
```

- [ ] **Step 3: 实现 status_bar widget**

替换 `src/tui/widgets/status_bar.rs`：

```rust
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
use ratatui::style::{Color, Style};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Paragraph, Widget};

pub struct StatusBar<'a> {
    pub hints: &'a [(&'a str, &'a str)],
}

impl<'a> Widget for StatusBar<'a> {
    fn render(self, area: Rect, buf: &mut Buffer) {
        let mut spans: Vec<Span> = Vec::new();
        for (i, (key, action)) in self.hints.iter().enumerate() {
            if i > 0 {
                spans.push(Span::raw("  "));
            }
            spans.push(Span::styled(*key, Style::default().fg(Color::Yellow)));
            spans.push(Span::raw(":"));
            spans.push(Span::raw(*action));
        }
        Paragraph::new(Line::from(spans)).render(area, buf);
    }
}
```

- [ ] **Step 4: 实现 main_view**

替换 `src/tui/views/main_view.rs`：

```rust
use ratatui::Frame;
use ratatui::layout::{Constraint, Direction, Layout};
use ratatui::widgets::ListState;

use crate::app::App;
use crate::tui::widgets::host_list::HostList;
use crate::tui::widgets::search_input::SearchInput;
use crate::tui::widgets::status_bar::StatusBar;

pub fn render(f: &mut Frame, app: &App, list_state: &mut ListState) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(3),
            Constraint::Min(1),
            Constraint::Length(1),
        ])
        .split(f.area());

    f.render_widget(
        SearchInput {
            query: &app.search_query,
            focused: true,
        },
        chunks[0],
    );

    f.render_stateful_widget(
        HostList {
            hosts: &app.hosts,
            indices: &app.filtered_indices,
        },
        chunks[1],
        list_state,
    );

    f.render_widget(
        StatusBar {
            hints: &[
                ("Enter", "SSH"),
                ("F2", "SFTP"),
                ("F5", "新建"),
                ("?", "帮助"),
                ("Q", "退出"),
            ],
        },
        chunks[2],
    );
}
```

- [ ] **Step 5: 接入 app.rs 的 draw**

修改 `src/app.rs` 中 `event_loop` 的 `terminal.draw` 闭包：

```rust
use ratatui::widgets::ListState;

// 在 App 结构体新增字段：
// pub list_state: ListState,

// App::new() 末尾 Self 初始化新增：
// list_state: ListState::default(),
```

具体：
1. `use ratatui::widgets::ListState;` 加到 app.rs 顶部
2. `App` struct 加 `pub list_state: ListState,`
3. `App::new()` 中初始化 `list_state: { let mut s = ListState::default(); if !filtered_indices.is_empty() { s.select(Some(0)); } s },`
4. `terminal.draw` 闭包替换为：

```rust
let app_ref = &*self;
terminal.draw(|f| {
    match self.mode {
        AppMode::Main => crate::tui::views::main_view::render(f, app_ref, &mut self.list_state),
        _ => {}
    }
})?;
```

（如遇借用冲突，把 `list_state` 从 `self` 中临时 take 出来：`let mut state = std::mem::take(&mut self.list_state);` 渲染后写回。）

- [ ] **Step 6: 添加上下键选中逻辑**

在 `handle_key` 中追加：

```rust
(KeyCode::Up, KeyModifiers::NONE) => self.select_previous(),
(KeyCode::Down, KeyModifiers::NONE) => self.select_next(),
```

在 `impl App` 增加：

```rust
fn select_next(&mut self) {
    if self.filtered_indices.is_empty() { return; }
    let i = self.list_state.selected().unwrap_or(0);
    let next = (i + 1).min(self.filtered_indices.len() - 1);
    self.list_state.select(Some(next));
}

fn select_previous(&mut self) {
    let i = self.list_state.selected().unwrap_or(0);
    let prev = i.saturating_sub(1);
    self.list_state.select(Some(prev));
}
```

- [ ] **Step 7: 准备种子数据用于手测**

临时在 `App::new()` 中：如果 `hosts` 为空，插入两条假数据：

```rust
let mut hosts = store::load_hosts()?;
if hosts.is_empty() {
    use crate::config::host::{Host, HostSource};
    hosts.push(Host {
        id: "demo-1".into(), alias: "demo-1".into(),
        hostname: "192.168.1.10".into(), port: 22,
        user: "u".into(), identity_files: vec![], proxy_jump: None,
        tags: vec!["web".into(), "prod".into()],
        description: String::new(), source: HostSource::Manual,
    });
    hosts.push(Host {
        id: "demo-2".into(), alias: "demo-2".into(),
        hostname: "10.0.0.5".into(), port: 22,
        user: "u".into(), identity_files: vec![], proxy_jump: None,
        tags: vec!["dev".into()],
        description: String::new(), source: HostSource::Manual,
    });
}
```

（Task 6 后这段种子数据会被 ssh_config 导入替代，届时删除。）

- [ ] **Step 8: 手动验证 UI**

```bash
cargo run
```
预期：
- 顶部搜索框（带 `> 搜索...`）
- 中间主机列表，`demo-1` 和 `demo-2` 可见，首行有 `● ` 标记
- 上下键能移动选中
- 底部 `Enter:SSH  F2:SFTP  F5:新建  ?:帮助  Q:退出`
- 按 `q` 退出，终端恢复正常

- [ ] **Step 9: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 10: Commit**

```bash
git add src/tui src/app.rs
git commit -m "feat(tui): 主界面渲染（搜索框 + 主机列表 + 快捷键栏）"
```

---

### Task 6: 搜索交互 + ssh config 自动导入（T6 扩展 F1）

**Files:**
- Modify: `src/app.rs`
- Modify: `src/tui/views/main_view.rs`（仅高亮用，可选）

**目标：**
1. 主界面文字输入（可打字、Backspace）实时过滤列表
2. 启动时自动调用 `ssh_config::import_ssh_config` → `store::merge_ssh_config_hosts` → `save_hosts`
3. 去掉 Task 5 临时加的 demo 种子数据

- [ ] **Step 1: 接入 ssh config 导入**

替换 `App::new()` 的初始化逻辑：

```rust
pub fn new() -> Result<Self> {
    let (existing, prev_hash) = store::load_from(&store::config_path())?;
    let (imported, new_hash) = crate::config::ssh_config::import_ssh_config()?;

    let hosts = if !new_hash.is_empty() && new_hash != prev_hash {
        let merged = store::merge_ssh_config_hosts(existing, imported);
        store::save_to(&store::config_path(), &merged, &new_hash)?;
        merged
    } else {
        existing
    };

    let filtered_indices: Vec<usize> = (0..hosts.len()).collect();
    let mut list_state = ratatui::widgets::ListState::default();
    if !filtered_indices.is_empty() {
        list_state.select(Some(0));
    }

    Ok(Self {
        mode: AppMode::Main,
        hosts,
        search_query: String::new(),
        filtered_indices,
        selected_index: 0,
        should_quit: false,
        list_state,
    })
}
```

（移除 Task 5 的 demo 种子数据。）

- [ ] **Step 2: 实现搜索输入处理**

在 `handle_key` 中在 `q`/`Ctrl-C` 分支之后追加（注意顺序：`q` 既是退出键也是字符，要区分上下文——先让 `Ctrl-C` 清空搜索再决定退出）：

```rust
(KeyCode::Char('c'), KeyModifiers::CONTROL) => {
    if self.search_query.is_empty() {
        self.should_quit = true;
    } else {
        self.search_query.clear();
        self.apply_search();
    }
}
(KeyCode::Backspace, KeyModifiers::NONE) => {
    self.search_query.pop();
    self.apply_search();
}
(KeyCode::Char(c), m) if m == KeyModifiers::NONE || m == KeyModifiers::SHIFT => {
    // 主界面模式下接受字符输入
    self.search_query.push(c);
    self.apply_search();
}
```

**注意：** 把之前的 `(KeyCode::Char('q'), KeyModifiers::NONE) => self.should_quit = true` 删除——v0.1 主界面退出统一用 `Ctrl-C`（搜索空）或上游脱出（超出 MVP 范围则保留 `q`）。依照 REQUIREMENTS.md F6：主界面单次 `Ctrl-C` 清空搜索；搜索框为空时 `Ctrl-C` 退出程序。

- [ ] **Step 3: 实现 apply_search**

在 `impl App` 中追加：

```rust
fn apply_search(&mut self) {
    self.filtered_indices =
        crate::utils::fuzzy::search(&self.search_query, &self.hosts);
    let sel = if self.filtered_indices.is_empty() { None } else { Some(0) };
    self.list_state.select(sel);
}
```

- [ ] **Step 4: 手动验证**

先确保 `~/.ssh/config` 中有至少 1 个具名 Host。运行：

```bash
cargo run
```
预期：
- 启动显示 ssh config 中的真实主机
- 输入字符实时过滤列表
- Backspace 删除字符
- `Ctrl-C`（搜索不为空）清空搜索
- `Ctrl-C`（搜索为空）退出

- [ ] **Step 5: 写一个集成单测**

在 `src/app.rs` 末尾（或新建 `tests/` 目录）追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::config::host::{Host, HostSource};

    fn app_with(hosts: Vec<Host>) -> App {
        App {
            mode: AppMode::Main,
            hosts,
            search_query: String::new(),
            filtered_indices: vec![],
            selected_index: 0,
            should_quit: false,
            list_state: ratatui::widgets::ListState::default(),
        }
    }

    #[test]
    fn search_filters_hosts() {
        let mut app = app_with(vec![
            Host {
                id: "web".into(), alias: "web".into(),
                hostname: "1".into(), port: 22, user: "u".into(),
                identity_files: vec![], proxy_jump: None,
                tags: vec![], description: String::new(),
                source: HostSource::Manual,
            },
            Host {
                id: "db".into(), alias: "db".into(),
                hostname: "2".into(), port: 22, user: "u".into(),
                identity_files: vec![], proxy_jump: None,
                tags: vec![], description: String::new(),
                source: HostSource::Manual,
            },
        ]);
        app.search_query = "web".into();
        app.apply_search();
        assert_eq!(app.filtered_indices.len(), 1);
        assert_eq!(app.hosts[app.filtered_indices[0]].alias, "web");
    }
}
```

```bash
cargo test --lib app::tests
```
预期：PASS。

- [ ] **Step 6: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 7: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): 启动导入 ssh config + 主界面实时模糊搜索"
```

---

## 阶段 3：SSH 连接（T7–T10）

> **测试说明：** 这一阶段难以纯单元测试。建议至少准备一个本地可控的 SSH 测试目标（如 Docker 启动 `linuxserver/openssh-server`，或本机 `sshd`），用来手测。

### Task 7: SSH 认证流程（T7，依赖 T8）

**Files:**
- Modify: `src/ssh/auth.rs`
- Modify: `src/ssh/session.rs`

**目标：** 实现认证链 ssh-agent → IdentityFile → password。先只做骨架，密码分支返回占位错误，待 Task 8 完成后由上层提供密码 callback。

- [ ] **Step 1: 实现 russh 客户端 Handler**

替换 `src/ssh/session.rs`：

```rust
use anyhow::{Context, Result};
use russh::client::{self, Handle, Msg};
use russh::keys::{PrivateKeyWithHashAlg, load_secret_key};
use russh::{Channel, ChannelMsg};
use std::path::Path;
use std::sync::Arc;
use tokio::sync::mpsc;

pub struct ClientHandler;

#[async_trait::async_trait]
impl client::Handler for ClientHandler {
    type Error = anyhow::Error;

    async fn check_server_key(
        &mut self,
        _server_public_key: &russh::keys::ssh_key::PublicKey,
    ) -> Result<bool, Self::Error> {
        // v0.1：一律接受（TOFU 机制见 v0.3）
        Ok(true)
    }
}

pub struct ActiveSession {
    pub handle: Handle<ClientHandler>,
    pub pty: Option<Channel<Msg>>,
}

impl ActiveSession {
    pub async fn connect(
        host: &str,
        port: u16,
        user: &str,
        auth: AuthStrategy,
    ) -> Result<Self> {
        let config = Arc::new(client::Config::default());
        let mut handle = client::connect(config, (host, port), ClientHandler)
            .await
            .with_context(|| format!("连接 {host}:{port} 失败"))?;

        let ok = auth.authenticate(&mut handle, user).await?;
        if !ok {
            anyhow::bail!("认证失败");
        }

        Ok(Self { handle, pty: None })
    }
}

pub enum AuthStrategy {
    // Task 7 最小实现：agent + 给定 key 路径
    Agent,
    Key { path: std::path::PathBuf, passphrase: Option<String> },
    Password(String),
}

impl AuthStrategy {
    pub async fn authenticate(
        &self,
        handle: &mut Handle<ClientHandler>,
        user: &str,
    ) -> Result<bool> {
        match self {
            Self::Agent => try_agent(handle, user).await,
            Self::Key { path, passphrase } => {
                try_key(handle, user, path, passphrase.as_deref()).await
            }
            Self::Password(p) => Ok(handle.authenticate_password(user, p).await?),
        }
    }
}

async fn try_agent(handle: &mut Handle<ClientHandler>, user: &str) -> Result<bool> {
    let mut agent = match russh::keys::agent::client::AgentClient::connect_env().await {
        Ok(a) => a,
        Err(_) => return Ok(false),
    };
    let identities = agent.request_identities().await.unwrap_or_default();
    for key in identities {
        let res = handle
            .authenticate_publickey_with(user, key, None, &mut agent)
            .await;
        if let Ok(auth) = res {
            if auth.success() {
                return Ok(true);
            }
        }
    }
    Ok(false)
}

async fn try_key(
    handle: &mut Handle<ClientHandler>,
    user: &str,
    path: &Path,
    passphrase: Option<&str>,
) -> Result<bool> {
    let key = load_secret_key(path, passphrase)
        .with_context(|| format!("加载私钥失败: {}", path.display()))?;
    let wrapped = PrivateKeyWithHashAlg::new(Arc::new(key), None);
    let auth = handle.authenticate_publickey(user, wrapped).await?;
    Ok(auth.success())
}
```

依赖说明：`async-trait = "0.1"` 需要加到 `Cargo.toml`：

```toml
async-trait = "0.1"
```

> **API 提示：** russh 0.60 的 `authenticate_publickey`、`authenticate_password`、`AgentClient` 的具体签名以 `cargo doc -p russh --open` 为准。以上代码基于 0.60 文档但可能需小幅调整。

- [ ] **Step 2: 实现 auth.rs 的策略编排**

替换 `src/ssh/auth.rs`：

```rust
use anyhow::Result;
use std::path::PathBuf;

use super::session::{ActiveSession, AuthStrategy};
use crate::config::host::Host;

/// 密码提示回调：由 TUI 层提供（Task 8 密码弹窗）
pub type PasswordPrompt = Box<dyn FnMut(&str) -> Option<String> + Send>;

pub async fn connect_with_host(
    host: &Host,
    mut prompt_password: PasswordPrompt,
) -> Result<ActiveSession> {
    // 1) agent
    if let Ok(s) = ActiveSession::connect(
        &host.hostname,
        host.port,
        &host.user,
        AuthStrategy::Agent,
    )
    .await
    {
        return Ok(s);
    }

    // 2) IdentityFile 序列
    for key_path in &host.identity_files {
        let expanded = expand_tilde(key_path);
        // 先尝试无密码
        if let Ok(s) = ActiveSession::connect(
            &host.hostname,
            host.port,
            &host.user,
            AuthStrategy::Key { path: expanded.clone(), passphrase: None },
        )
        .await
        {
            return Ok(s);
        }
        // 需要 passphrase：提示用户输入
        if let Some(pass) = prompt_password(&format!("解锁 {}", expanded.display())) {
            if let Ok(s) = ActiveSession::connect(
                &host.hostname,
                host.port,
                &host.user,
                AuthStrategy::Key { path: expanded, passphrase: Some(pass) },
            )
            .await
            {
                return Ok(s);
            }
        }
    }

    // 3) 密码认证
    if let Some(pass) = prompt_password(&format!("{}@{} 密码", host.user, host.hostname)) {
        if let Ok(s) = ActiveSession::connect(
            &host.hostname,
            host.port,
            &host.user,
            AuthStrategy::Password(pass),
        )
        .await
        {
            return Ok(s);
        }
    }

    anyhow::bail!("所有认证方式均失败");
}

fn expand_tilde(p: &PathBuf) -> PathBuf {
    let s = p.to_string_lossy();
    if let Some(rest) = s.strip_prefix("~/") {
        if let Some(home) = dirs::home_dir() {
            return home.join(rest);
        }
    }
    p.clone()
}
```

- [ ] **Step 3: cargo check**

```bash
cargo check
```
预期：通过。如果 russh API 签名不符，逐项调整，但不要改变 AuthStrategy 的语义。

- [ ] **Step 4: Commit**

```bash
git add Cargo.toml Cargo.lock src/ssh
git commit -m "feat(ssh): 认证策略链（agent → key → password）"
```

---

### Task 8: 密码输入弹窗（T8）

**Files:**
- Create: `src/tui/views/password_dialog.rs`
- Modify: `src/tui/views/mod.rs`

**目标：** 一个遮罩对话框：居中绘制，输入字符显示为 `*`，Enter 确认、Esc 取消。由 `App` 在需要时调用（Task 9 整合）。

- [ ] **Step 1: 创建 password_dialog.rs**

新建 `src/tui/views/password_dialog.rs`：

```rust
use ratatui::Frame;
use ratatui::layout::{Alignment, Constraint, Direction, Layout, Rect};
use ratatui::style::{Color, Style};
use ratatui::widgets::{Block, Borders, Clear, Paragraph};

pub struct PasswordDialog {
    pub title: String,
    pub input: String,
}

impl PasswordDialog {
    pub fn new(title: impl Into<String>) -> Self {
        Self { title: title.into(), input: String::new() }
    }

    pub fn render(&self, f: &mut Frame) {
        let area = centered_rect(50, 20, f.area());
        f.render_widget(Clear, area);
        let masked: String = "*".repeat(self.input.chars().count());
        let block = Block::default()
            .title(format!(" {} ", self.title))
            .borders(Borders::ALL)
            .style(Style::default().fg(Color::Yellow));
        let inner = Layout::default()
            .direction(Direction::Vertical)
            .margin(1)
            .constraints([Constraint::Length(1), Constraint::Length(1)])
            .split(block.inner(area));
        f.render_widget(block, area);
        f.render_widget(
            Paragraph::new(masked).alignment(Alignment::Left),
            inner[0],
        );
        f.render_widget(
            Paragraph::new("Enter:确认  Esc:取消")
                .style(Style::default().fg(Color::DarkGray)),
            inner[1],
        );
    }
}

fn centered_rect(percent_x: u16, percent_y: u16, r: Rect) -> Rect {
    let popup_layout = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Percentage((100 - percent_y) / 2),
            Constraint::Percentage(percent_y),
            Constraint::Percentage((100 - percent_y) / 2),
        ])
        .split(r);
    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage((100 - percent_x) / 2),
            Constraint::Percentage(percent_x),
            Constraint::Percentage((100 - percent_x) / 2),
        ])
        .split(popup_layout[1])[1]
}
```

- [ ] **Step 2: 注册模块**

编辑 `src/tui/views/mod.rs`：

```rust
pub mod main_view;
pub mod password_dialog;
pub mod sftp_view;
```

- [ ] **Step 3: cargo check**

```bash
cargo check
```
预期：通过。

- [ ] **Step 4: Commit**

```bash
git add src/tui/views
git commit -m "feat(tui): 密码输入弹窗组件"
```

---

### Task 9: SSH 接管模式（T9）

**Files:**
- Modify: `src/app.rs`
- Modify: `src/ssh/session.rs`
- Modify: `src/tui/event.rs`

**目标：** 按 Enter 在主界面 → 进入 SSH 接管模式：
- 离开 ratatui 渲染（可选做法：保持 alternate screen + 让 SSH 接管全屏）
- 打开 PTY channel，stdin 转发到远程，远程输出写到 stdout
- `exit` / `Ctrl-D` 关闭 channel → 返回主界面
- 密码弹窗流程（Task 8）接入认证

**设计简化（v0.1）：** 进入 SSH 模式时我们暂时退出 ratatui 的渲染循环，把 stdin/stdout 直接连到 PTY channel；退出后再重新进入渲染循环。相比在 ratatui 中嵌入伪终端更简单可靠。

- [ ] **Step 1: 扩展 ActiveSession 打开 PTY**

在 `src/ssh/session.rs` 的 `impl ActiveSession` 追加：

```rust
pub async fn request_pty(&mut self, term: &str, cols: u16, rows: u16) -> Result<()> {
    let mut ch = self.handle.channel_open_session().await?;
    ch.request_pty(
        false,
        term,
        cols as u32,
        rows as u32,
        0,
        0,
        &[],
    ).await?;
    ch.request_shell(false).await?;
    self.pty = Some(ch);
    Ok(())
}
```

- [ ] **Step 2: 实现接管循环**

在 `src/ssh/session.rs` 继续追加：

```rust
use tokio::io::{AsyncReadExt, AsyncWriteExt};

impl ActiveSession {
    /// 进入 SSH 接管模式。从 stdin 读字节转发到远程，
    /// 从远程读数据写到 stdout。
    /// 返回：Ok(true) 表示用户按前缀键触发模式切换；
    /// Ok(false) 表示远程关闭（exit/Ctrl-D）；
    /// Err 表示异常。
    pub async fn takeover(&mut self, switch_byte: u8) -> Result<bool> {
        use tokio::io::stdin;
        let ch = self.pty.as_mut().context("PTY 未建立")?;
        let mut stdin = stdin();
        let mut buf = [0u8; 4096];
        loop {
            tokio::select! {
                n = stdin.read(&mut buf) => {
                    let n = n?;
                    if n == 0 { break; }
                    // 扫描前缀键
                    if buf[..n].contains(&switch_byte) {
                        return Ok(true);
                    }
                    ch.data(&buf[..n]).await?;
                }
                msg = ch.wait() => {
                    match msg {
                        Some(ChannelMsg::Data { ref data }) => {
                            tokio::io::stdout().write_all(data).await?;
                            tokio::io::stdout().flush().await?;
                        }
                        Some(ChannelMsg::ExtendedData { ref data, .. }) => {
                            tokio::io::stderr().write_all(data).await?;
                            tokio::io::stderr().flush().await?;
                        }
                        Some(ChannelMsg::Eof) | Some(ChannelMsg::Close) => break,
                        _ => {}
                    }
                }
            }
        }
        Ok(false)
    }
}
```

- [ ] **Step 3: App 中响应 Enter**

在 `handle_key` 的主界面分支中追加：

```rust
(KeyCode::Enter, KeyModifiers::NONE) => {
    self.trigger_connect = true; // 用 flag，实际 connect 在异步上下文里做
}
```

在 `App` 结构体加 `pub trigger_connect: bool,`（默认 false），并在 `event_loop` 循环体开头：

```rust
if self.trigger_connect {
    self.trigger_connect = false;
    if let Some(&idx) = self.filtered_indices.get(
        self.list_state.selected().unwrap_or(0)
    ) {
        let host = self.hosts[idx].clone();
        // 暂停 ratatui
        restore_terminal(terminal)?;
        match self.ssh_takeover(&host).await {
            Ok(()) => {}
            Err(e) => eprintln!("\n连接错误: {e}\n按回车返回..."),
        }
        // 重新进入 TUI
        *terminal = setup_terminal()?;
    }
}
```

- [ ] **Step 4: 实现 ssh_takeover**

在 `impl App` 追加：

```rust
async fn ssh_takeover(&mut self, host: &Host) -> Result<()> {
    use crate::ssh::auth::connect_with_host;

    // 简化：密码弹窗在 v0.1 初版用 stdin 提示（交互不佳但稳妥）。
    // Task 10 之后替换为真正的 TUI 密码弹窗。
    let prompt: crate::ssh::auth::PasswordPrompt =
        Box::new(|title: &str| -> Option<String> {
            eprint!("{title}: ");
            use std::io::Write;
            let _ = std::io::stderr().flush();
            let mut s = String::new();
            std::io::stdin().read_line(&mut s).ok()?;
            Some(s.trim_end_matches('\n').to_string())
        });
    let mut session = connect_with_host(host, prompt).await?;

    let (cols, rows) = crossterm::terminal::size().unwrap_or((80, 24));
    session.request_pty("xterm-256color", cols, rows).await?;

    // 进入 raw mode 透传
    crossterm::terminal::enable_raw_mode()?;
    let result = session.takeover(0x00).await;
    crossterm::terminal::disable_raw_mode()?;

    match result? {
        true => {
            // 前缀键触发 → Task 10 之后切到 SFTP；v0.1 初期先返回主界面
            self.mode = AppMode::Main;
        }
        false => {
            self.mode = AppMode::Main;
        }
    }
    Ok(())
}
```

（`use crate::config::host::Host;` 如未 import，顶部补上。）

- [ ] **Step 5: 手动验证**

准备一台可 SSH 登录的测试机（例如本机 `sshd`，`~/.ssh/config` 中加一个 `Host localhost-test`）。

```bash
cargo run
```
操作：选中主机 → Enter → 看到认证交互（若密钥无加密，agent 会自动通过）→ 进入远程 shell → 执行命令 → `exit` → 返回主界面。

- [ ] **Step 6: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 7: Commit**

```bash
git add src
git commit -m "feat(ssh): 接管模式 I/O 转发（Enter 连接，exit/Ctrl-D 返回）"
```

---

### Task 10: 前缀键 + 终端 resize（T10）

**Files:**
- Modify: `src/ssh/session.rs`

**目标：**
1. `takeover` 已处理 0x00 触发 switch_byte；现在让 SIGWINCH / resize 事件被感知并通过 `window_change` 转发给远程
2. 添加注释说明 IXON 禁用依赖点（crossterm 进入 raw mode 已处理）

- [ ] **Step 1: resize 处理**

在 `takeover` 函数的 `tokio::select!` 增加一个分支。需要接收来自主循环的 resize 事件 —— 用 `tokio::sync::watch` 传递：

修改 `takeover` 签名，增加 resize 通道：

```rust
pub async fn takeover(
    &mut self,
    switch_byte: u8,
    mut resize_rx: tokio::sync::watch::Receiver<(u16, u16)>,
) -> Result<bool> {
    // ... 原有代码 ...
    // 新增分支：
    // resize_rx.changed() => {
    //     let (cols, rows) = *resize_rx.borrow();
    //     ch.window_change(cols as u32, rows as u32, 0, 0).await?;
    // }
}
```

完整 takeover 改为：

```rust
pub async fn takeover(
    &mut self,
    switch_byte: u8,
    mut resize_rx: tokio::sync::watch::Receiver<(u16, u16)>,
) -> Result<bool> {
    use tokio::io::stdin;
    let ch = self.pty.as_mut().context("PTY 未建立")?;
    let mut stdin = stdin();
    let mut buf = [0u8; 4096];
    loop {
        tokio::select! {
            n = stdin.read(&mut buf) => {
                let n = n?;
                if n == 0 { break; }
                if buf[..n].contains(&switch_byte) {
                    return Ok(true);
                }
                ch.data(&buf[..n]).await?;
            }
            msg = ch.wait() => {
                match msg {
                    Some(ChannelMsg::Data { ref data }) => {
                        tokio::io::stdout().write_all(data).await?;
                        tokio::io::stdout().flush().await?;
                    }
                    Some(ChannelMsg::ExtendedData { ref data, .. }) => {
                        tokio::io::stderr().write_all(data).await?;
                        tokio::io::stderr().flush().await?;
                    }
                    Some(ChannelMsg::Eof) | Some(ChannelMsg::Close) => break,
                    _ => {}
                }
            }
            Ok(()) = resize_rx.changed() => {
                let (cols, rows) = *resize_rx.borrow();
                ch.window_change(cols as u32, rows as u32, 0, 0).await?;
            }
        }
    }
    Ok(false)
}
```

- [ ] **Step 2: App 中启动 resize 监听**

在 `ssh_takeover` 中创建 watch channel + 一个任务订阅 crossterm resize：

```rust
let (rtx, rrx) = tokio::sync::watch::channel(
    crossterm::terminal::size().unwrap_or((80, 24))
);
let _resize_task = tokio::spawn(async move {
    use crossterm::event::EventStream;
    use futures::StreamExt;
    let mut stream = EventStream::new();
    while let Some(Ok(ev)) = stream.next().await {
        if let crossterm::event::Event::Resize(w, h) = ev {
            let _ = rtx.send((w, h));
        }
    }
});
// 接管时传入 rrx
let result = session.takeover(0x00, rrx).await;
// takeover 结束时 drop _resize_task（未自动停止也无妨，下轮会被替换）
```

注意：并发读 stdin 的事件流与我们自己的 `stdin.read` 可能冲突。简化方案：接管期间不订阅 crossterm 事件，改为在 `stdin.read` 分支里发现 `SIGWINCH` 自行 poll 一次 `terminal::size()`。

**更稳妥方案**：每次 `stdin.read` 返回 0 字节或特定标志后调用 `crossterm::terminal::size()` 比较并发送。但最简单方式是每 500ms tick 检查一次 size 变化：

```rust
let mut size_ticker = tokio::time::interval(std::time::Duration::from_millis(500));
let mut last_size = crossterm::terminal::size().unwrap_or((80, 24));
// 在 select! 中增加：
// _ = size_ticker.tick() => {
//     let now = crossterm::terminal::size().unwrap_or(last_size);
//     if now != last_size {
//         ch.window_change(now.0 as u32, now.1 as u32, 0, 0).await?;
//         last_size = now;
//     }
// }
```

采用后者，省掉 watch channel。更新 `takeover` 签名去掉 `resize_rx`，直接在内部 poll。

**最终版 takeover**：

```rust
pub async fn takeover(&mut self, switch_byte: u8) -> Result<bool> {
    use tokio::io::stdin;
    let ch = self.pty.as_mut().context("PTY 未建立")?;
    let mut stdin = stdin();
    let mut buf = [0u8; 4096];
    let mut size_ticker = tokio::time::interval(std::time::Duration::from_millis(500));
    let mut last_size = crossterm::terminal::size().unwrap_or((80, 24));
    loop {
        tokio::select! {
            n = stdin.read(&mut buf) => {
                let n = n?;
                if n == 0 { break; }
                if buf[..n].contains(&switch_byte) {
                    return Ok(true);
                }
                ch.data(&buf[..n]).await?;
            }
            msg = ch.wait() => {
                match msg {
                    Some(ChannelMsg::Data { ref data }) => {
                        tokio::io::stdout().write_all(data).await?;
                        tokio::io::stdout().flush().await?;
                    }
                    Some(ChannelMsg::ExtendedData { ref data, .. }) => {
                        tokio::io::stderr().write_all(data).await?;
                        tokio::io::stderr().flush().await?;
                    }
                    Some(ChannelMsg::Eof) | Some(ChannelMsg::Close) => break,
                    _ => {}
                }
            }
            _ = size_ticker.tick() => {
                let now = crossterm::terminal::size().unwrap_or(last_size);
                if now != last_size {
                    ch.window_change(now.0 as u32, now.1 as u32, 0, 0).await?;
                    last_size = now;
                }
            }
        }
    }
    Ok(false)
}
```

- [ ] **Step 3: 手测 resize 与前缀键**

```bash
cargo run
```
连入远程后：
1. 运行 `top` 或 `htop`，缩放终端 → 界面应同步调整
2. 按 `Ctrl-Space`（产生 `0x00`）→ 返回主界面（Task 11 后切到 SFTP）

- [ ] **Step 4: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 5: Commit**

```bash
git add src/ssh/session.rs src/app.rs
git commit -m "feat(ssh): 接管期间同步终端 resize 与前缀键识别"
```

---

## 阶段 4：SFTP（T11–T13）

### Task 11: SFTP 客户端封装（T11）

**Files:**
- Modify: `src/sftp/client.rs`
- Modify: `src/ssh/session.rs`（复用同一连接）

**目标：** 从 `ActiveSession` 打开 SFTP subsystem channel，封装 `list_dir(path)`、`read_attr(path)` 接口。复用同一 TCP 连接。

- [ ] **Step 1: 实现 SftpClient**

替换 `src/sftp/client.rs`：

```rust
use anyhow::{Context, Result};
use russh_sftp::client::SftpSession;
use russh_sftp::protocol::FileType;

use crate::ssh::session::ActiveSession;

pub struct SftpClient {
    pub session: SftpSession,
}

#[derive(Debug, Clone)]
pub struct FileEntry {
    pub name: String,
    pub is_dir: bool,
    pub size: u64,
}

impl SftpClient {
    pub async fn open(ssh: &ActiveSession) -> Result<Self> {
        let channel = ssh.handle.channel_open_session().await?;
        channel.request_subsystem(true, "sftp").await?;
        let sftp = SftpSession::new(channel.into_stream()).await
            .context("打开 SFTP subsystem 失败")?;
        Ok(Self { session: sftp })
    }

    pub async fn list_dir(&self, path: &str) -> Result<Vec<FileEntry>> {
        let entries = self.session.read_dir(path).await
            .with_context(|| format!("列目录失败: {path}"))?;
        let mut result: Vec<FileEntry> = entries
            .into_iter()
            .map(|e| FileEntry {
                name: e.file_name().to_string(),
                is_dir: matches!(e.file_type(), FileType::Dir),
                size: e.metadata().size.unwrap_or(0),
            })
            .collect();
        // 目录在前、文件在后，各自按名称升序
        result.sort_by(|a, b| match (a.is_dir, b.is_dir) {
            (true, false) => std::cmp::Ordering::Less,
            (false, true) => std::cmp::Ordering::Greater,
            _ => a.name.cmp(&b.name),
        });
        Ok(result)
    }

    pub async fn home_dir(&self) -> String {
        // SFTP 协议无内置 home，用 "." 让 russh-sftp 解析
        self.session.canonicalize(".").await.unwrap_or_else(|_| "/".into())
    }
}
```

> **API 注意：** `russh-sftp` 2.x 的 `SftpSession::new` 可能要求 `ChannelStream`。若 `channel.into_stream()` 不存在，查阅 `russh-sftp` docs。可能需要使用 `russh_sftp::client::SftpSession::new(channel).await` 或通过 `RawChannelStream` 包装。

- [ ] **Step 2: cargo check**

```bash
cargo check
```
调整 API 直到通过。

- [ ] **Step 3: 手测（可选）**

写一个 `examples/sftp_ls.rs`（仅本地测试，不 commit）：

```rust
// examples/sftp_ls.rs
// cargo run --example sftp_ls
// 验证：列出远程 $HOME 后输出条目
```

跳过也可，Task 12 的 UI 会立即用到。

- [ ] **Step 4: Commit**

```bash
git add src/sftp/client.rs
git commit -m "feat(sftp): 复用 SSH 连接的 SFTP client，封装 list_dir"
```

---

### Task 12: SFTP 文件浏览器 UI（T12）

**Files:**
- Modify: `src/tui/widgets/file_list.rs`
- Modify: `src/tui/views/sftp_view.rs`
- Modify: `src/app.rs`

**目标：**
- 单面板文件浏览器
- 显示目录（`<DIR>`）、文件（人类可读大小）
- 顶部路径栏
- 底部快捷键栏 `Tab:本地/远程 F5:下载 F6:上传 Ctrl-Space:SSH Q:返回`
- Enter 进入目录 / `..` 返回上级
- Tab 切本地/远程视图

- [ ] **Step 1: 实现 file_list widget**

替换 `src/tui/widgets/file_list.rs`：

```rust
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
use ratatui::style::{Color, Modifier, Style};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, List, ListItem, ListState, StatefulWidget};

use crate::sftp::client::FileEntry;

pub struct FileList<'a> {
    pub entries: &'a [FileEntry],
    pub title: &'a str,
}

impl<'a> StatefulWidget for FileList<'a> {
    type State = ListState;
    fn render(self, area: Rect, buf: &mut Buffer, state: &mut Self::State) {
        let items: Vec<ListItem> = self
            .entries
            .iter()
            .map(|e| {
                let size = if e.is_dir {
                    "<DIR>".to_string()
                } else {
                    human_size(e.size)
                };
                let prefix = if e.is_dir { "▸ " } else { "  " };
                ListItem::new(Line::from(vec![
                    Span::raw(prefix),
                    Span::styled(
                        format!("{:<40}", e.name),
                        if e.is_dir {
                            Style::default().fg(Color::Cyan)
                        } else {
                            Style::default()
                        },
                    ),
                    Span::raw(size),
                ]))
            })
            .collect();
        List::new(items)
            .block(Block::bordered().title(self.title.to_string()))
            .highlight_style(Style::default().add_modifier(Modifier::REVERSED))
            .render(area, buf, state);
    }
}

fn human_size(n: u64) -> String {
    const UNITS: &[&str] = &["B", "KB", "MB", "GB", "TB"];
    let mut v = n as f64;
    let mut u = 0;
    while v >= 1024.0 && u < UNITS.len() - 1 {
        v /= 1024.0;
        u += 1;
    }
    if u == 0 {
        format!("{} {}", n, UNITS[u])
    } else {
        format!("{:.1} {}", v, UNITS[u])
    }
}
```

- [ ] **Step 2: SftpPane 状态类型**

在 `src/sftp/mod.rs` 加：

```rust
pub mod client;
pub mod transfer;

use crate::sftp::client::FileEntry;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PaneSide {
    Local,
    Remote,
}

pub struct SftpPaneState {
    pub side: PaneSide,
    pub local_path: std::path::PathBuf,
    pub remote_path: String,
    pub local_entries: Vec<FileEntry>,
    pub remote_entries: Vec<FileEntry>,
    pub list_state: ratatui::widgets::ListState,
}

impl SftpPaneState {
    pub fn new(remote_home: String) -> Self {
        let mut ls = ratatui::widgets::ListState::default();
        ls.select(Some(0));
        Self {
            side: PaneSide::Remote,
            local_path: std::env::current_dir().unwrap_or_default(),
            remote_path: remote_home,
            local_entries: Vec::new(),
            remote_entries: Vec::new(),
            list_state: ls,
        }
    }
}
```

- [ ] **Step 3: sftp_view 渲染**

替换 `src/tui/views/sftp_view.rs`：

```rust
use ratatui::Frame;
use ratatui::layout::{Constraint, Direction, Layout};
use ratatui::widgets::Paragraph;

use crate::sftp::{PaneSide, SftpPaneState};
use crate::tui::widgets::file_list::FileList;
use crate::tui::widgets::status_bar::StatusBar;

pub fn render(f: &mut Frame, host_alias: &str, pane: &mut SftpPaneState) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(1),
            Constraint::Min(1),
            Constraint::Length(1),
        ])
        .split(f.area());

    let (label, path) = match pane.side {
        PaneSide::Local => ("本地", pane.local_path.display().to_string()),
        PaneSide::Remote => ("远程", pane.remote_path.clone()),
    };
    f.render_widget(
        Paragraph::new(format!(" SFTP: {host_alias}  [{label}] {path}")),
        chunks[0],
    );

    let entries = match pane.side {
        PaneSide::Local => &pane.local_entries,
        PaneSide::Remote => &pane.remote_entries,
    };
    f.render_stateful_widget(
        FileList { entries, title: label },
        chunks[1],
        &mut pane.list_state,
    );

    f.render_widget(
        StatusBar {
            hints: &[
                ("Tab", "本地/远程"),
                ("F5", "下载"),
                ("F6", "上传"),
                ("Ctrl-Space", "SSH"),
                ("Q", "返回"),
            ],
        },
        chunks[2],
    );
}
```

- [ ] **Step 4: 本地目录扫描辅助**

在 `src/sftp/client.rs` 追加：

```rust
pub fn list_local(path: &std::path::Path) -> anyhow::Result<Vec<FileEntry>> {
    let mut entries: Vec<FileEntry> = std::fs::read_dir(path)?
        .filter_map(|e| e.ok())
        .map(|e| {
            let meta = e.metadata().ok();
            FileEntry {
                name: e.file_name().to_string_lossy().into_owned(),
                is_dir: meta.as_ref().map(|m| m.is_dir()).unwrap_or(false),
                size: meta.as_ref().map(|m| m.len()).unwrap_or(0),
            }
        })
        .collect();
    entries.sort_by(|a, b| match (a.is_dir, b.is_dir) {
        (true, false) => std::cmp::Ordering::Less,
        (false, true) => std::cmp::Ordering::Greater,
        _ => a.name.cmp(&b.name),
    });
    Ok(entries)
}
```

- [ ] **Step 5: App 接入 SFTP 模式**

在 `App` 结构体加：

```rust
pub sftp_pane: Option<SftpPaneState>,
pub sftp_client: Option<SftpClient>,
pub ssh_session: Option<ActiveSession>,
```

在 `handle_key` 主界面分支加 `F2` 处理：

```rust
(KeyCode::F(2), _) => self.trigger_sftp = true,
```

在 `event_loop` 里对 `trigger_sftp` 做处理（类似 `trigger_connect`，但连接后建立 SFTP 并加载目录，而后进入 `AppMode::Sftp`）。

在 `terminal.draw` 内按 `self.mode` 分派：

```rust
match self.mode {
    AppMode::Main => main_view::render(f, &*self, &mut self.list_state),
    AppMode::Sftp => {
        if let Some(pane) = &mut self.sftp_pane {
            let alias = self.current_host_alias.as_deref().unwrap_or("");
            sftp_view::render(f, alias, pane);
        }
    }
    AppMode::Ssh => {} // 接管期间不走 ratatui
}
```

（为支持 `current_host_alias`，`App` 增加 `pub current_host_alias: Option<String>`。）

在 `handle_key` 的 SFTP 分支加：

```rust
AppMode::Sftp => match (k.code, k.modifiers) {
    (KeyCode::Char('q'), KeyModifiers::NONE) => self.exit_sftp(),
    (KeyCode::Tab, _) => self.toggle_pane(),
    (KeyCode::Up, _) => self.pane_select_prev(),
    (KeyCode::Down, _) => self.pane_select_next(),
    (KeyCode::Enter, _) => self.trigger_pane_enter = true,
    _ => {}
}
```

对应方法（略，与 main_view 的选择逻辑对称）。`trigger_pane_enter` 在 event_loop 中异步执行：若选中项是目录则 `list_dir(parent/name)`；`..` 则回上级。

> **注意：** `handle_key` 的分派要按 `self.mode` 区分——把现有的主界面 key 处理移到 `AppMode::Main` 分支内。

- [ ] **Step 6: 手测**

```bash
cargo run
```
在主界面选中有效主机 → F2 → 看到远程 `$HOME` 目录列表 → 上下键选择 → Enter 进入子目录 → 选中 `..` 回上级 → Tab 切到本地 → 看到 `$PWD`。

- [ ] **Step 7: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 8: Commit**

```bash
git add src
git commit -m "feat(sftp): 单面板文件浏览器（Tab 切本地/远程，Enter 进目录）"
```

---

### Task 13: 文件上传/下载 + 进度（T13）

**Files:**
- Modify: `src/sftp/transfer.rs`
- Modify: `src/tui/widgets/progress_bar.rs`
- Modify: `src/app.rs`

**目标：** F5 下载（远程选中文件 → 本地当前目录）、F6 上传（本地 → 远程当前目录）；底部进度条实时更新。32KB 缓冲区分块，每块通过 mpsc 发送进度。

- [ ] **Step 1: 数据结构**

替换 `src/sftp/transfer.rs`：

```rust
use anyhow::{Context, Result};
use russh_sftp::client::SftpSession;
use std::path::{Path, PathBuf};
use tokio::io::{AsyncReadExt, AsyncWriteExt};
use tokio::sync::mpsc;

#[derive(Debug, Clone)]
pub struct TransferProgress {
    pub filename: String,
    pub total_bytes: u64,
    pub transferred_bytes: u64,
    pub state: TransferState,
}

#[derive(Debug, Clone)]
pub enum TransferState {
    InProgress,
    Completed,
    Failed(String),
    Cancelled,
}

pub struct TransferHandle {
    pub rx: mpsc::Receiver<TransferProgress>,
    pub cancel: tokio_util::sync::CancellationToken,
}

const CHUNK: usize = 32 * 1024;

pub fn download(
    sftp: SftpSession,
    remote: String,
    local: PathBuf,
    total: u64,
) -> TransferHandle {
    let (tx, rx) = mpsc::channel(32);
    let cancel = tokio_util::sync::CancellationToken::new();
    let c2 = cancel.clone();
    tokio::spawn(async move {
        let r = do_download(&sftp, &remote, &local, total, tx.clone(), c2).await;
        if let Err(e) = r {
            let _ = tx.send(TransferProgress {
                filename: remote.clone(),
                total_bytes: total,
                transferred_bytes: 0,
                state: TransferState::Failed(e.to_string()),
            }).await;
        }
    });
    TransferHandle { rx, cancel }
}

async fn do_download(
    sftp: &SftpSession,
    remote: &str,
    local: &Path,
    total: u64,
    tx: mpsc::Sender<TransferProgress>,
    cancel: tokio_util::sync::CancellationToken,
) -> Result<()> {
    let filename = Path::new(remote)
        .file_name()
        .map(|s| s.to_string_lossy().into_owned())
        .unwrap_or_default();
    let mut rf = sftp.open(remote).await.context("打开远程文件失败")?;
    let mut lf = tokio::fs::File::create(local).await?;
    let mut buf = vec![0u8; CHUNK];
    let mut acc = 0u64;
    loop {
        if cancel.is_cancelled() {
            let _ = tx.send(TransferProgress {
                filename: filename.clone(),
                total_bytes: total,
                transferred_bytes: acc,
                state: TransferState::Cancelled,
            }).await;
            return Ok(());
        }
        let n = rf.read(&mut buf).await?;
        if n == 0 { break; }
        lf.write_all(&buf[..n]).await?;
        acc += n as u64;
        let _ = tx.send(TransferProgress {
            filename: filename.clone(),
            total_bytes: total,
            transferred_bytes: acc,
            state: TransferState::InProgress,
        }).await;
    }
    let _ = tx.send(TransferProgress {
        filename,
        total_bytes: total,
        transferred_bytes: acc,
        state: TransferState::Completed,
    }).await;
    Ok(())
}

pub fn upload(
    sftp: SftpSession,
    local: PathBuf,
    remote: String,
) -> Result<TransferHandle> {
    let total = std::fs::metadata(&local)?.len();
    let (tx, rx) = mpsc::channel(32);
    let cancel = tokio_util::sync::CancellationToken::new();
    let c2 = cancel.clone();
    tokio::spawn(async move {
        let r = do_upload(&sftp, &local, &remote, total, tx.clone(), c2).await;
        if let Err(e) = r {
            let _ = tx.send(TransferProgress {
                filename: remote.clone(),
                total_bytes: total,
                transferred_bytes: 0,
                state: TransferState::Failed(e.to_string()),
            }).await;
        }
    });
    Ok(TransferHandle { rx, cancel })
}

async fn do_upload(
    sftp: &SftpSession,
    local: &Path,
    remote: &str,
    total: u64,
    tx: mpsc::Sender<TransferProgress>,
    cancel: tokio_util::sync::CancellationToken,
) -> Result<()> {
    let filename = local
        .file_name()
        .map(|s| s.to_string_lossy().into_owned())
        .unwrap_or_default();
    let mut lf = tokio::fs::File::open(local).await?;
    let mut rf = sftp.create(remote).await.context("创建远程文件失败")?;
    let mut buf = vec![0u8; CHUNK];
    let mut acc = 0u64;
    loop {
        if cancel.is_cancelled() {
            let _ = tx.send(TransferProgress {
                filename: filename.clone(),
                total_bytes: total,
                transferred_bytes: acc,
                state: TransferState::Cancelled,
            }).await;
            return Ok(());
        }
        let n = lf.read(&mut buf).await?;
        if n == 0 { break; }
        rf.write_all(&buf[..n]).await?;
        acc += n as u64;
        let _ = tx.send(TransferProgress {
            filename: filename.clone(),
            total_bytes: total,
            transferred_bytes: acc,
            state: TransferState::InProgress,
        }).await;
    }
    let _ = tx.send(TransferProgress {
        filename,
        total_bytes: total,
        transferred_bytes: acc,
        state: TransferState::Completed,
    }).await;
    Ok(())
}
```

依赖新增：

```toml
tokio-util = "0.7"
```

> **API 提示：** `SftpSession::open`/`create` 返回的对象需支持 `AsyncRead`/`AsyncWrite`。若 russh-sftp 2.1 的 API 为 `session.read(path)` / `session.write(path, bytes)`（非 handle 式），改写：
> ```rust
> // 下载
> let data = sftp.read(remote).await?;   // 小文件
> // 分块：循环调用 sftp.read_chunk(handle, offset, len).await?
> ```
> 具体以 `cargo doc -p russh-sftp --open` 为准。目标不变：分块、可取消、进度推送。

- [ ] **Step 2: 进度条组件**

替换 `src/tui/widgets/progress_bar.rs`：

```rust
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
use ratatui::style::{Color, Style};
use ratatui::widgets::{Gauge, Widget};

use crate::sftp::transfer::TransferProgress;

pub struct ProgressView<'a> {
    pub progress: &'a TransferProgress,
    pub verb: &'a str, // "下载" / "上传"
}

impl<'a> Widget for ProgressView<'a> {
    fn render(self, area: Rect, buf: &mut Buffer) {
        let ratio = if self.progress.total_bytes == 0 {
            0.0
        } else {
            self.progress.transferred_bytes as f64 / self.progress.total_bytes as f64
        };
        let label = format!(
            "{} {}  {}/{}",
            self.verb,
            self.progress.filename,
            human(self.progress.transferred_bytes),
            human(self.progress.total_bytes),
        );
        Gauge::default()
            .gauge_style(Style::default().fg(Color::Green))
            .ratio(ratio.clamp(0.0, 1.0))
            .label(label)
            .render(area, buf);
    }
}

fn human(n: u64) -> String {
    const U: &[&str] = &["B", "KB", "MB", "GB"];
    let mut v = n as f64;
    let mut i = 0;
    while v >= 1024.0 && i < U.len() - 1 {
        v /= 1024.0;
        i += 1;
    }
    format!("{:.1} {}", v, U[i])
}
```

- [ ] **Step 3: App 集成**

在 `App` 加：

```rust
pub active_transfer: Option<ActiveTransfer>,

pub struct ActiveTransfer {
    pub verb: &'static str, // "下载" / "上传"
    pub progress: TransferProgress,
    pub handle: tokio::task::JoinHandle<()>,
    pub rx: tokio::sync::mpsc::Receiver<TransferProgress>,
    pub cancel: tokio_util::sync::CancellationToken,
}
```

SFTP 模式下 F5/F6 触发：

```rust
(KeyCode::F(5), _) => self.trigger_download = true,
(KeyCode::F(6), _) => self.trigger_upload = true,
```

`event_loop` 在每轮前：
1. 若 `active_transfer.is_some()`，`tokio::select!` 额外监听 `progress_rx`，更新 `progress` 字段；`Completed` / `Failed` / `Cancelled` 时 3 秒后清除
2. 下载/上传触发时，从 SFTP client 拿出 SftpSession 克隆给 transfer 任务（russh-sftp 允许 `SftpSession` 克隆，若不行则通过 Arc<Mutex> 共享，v0.1 简化：直接阻塞 SFTP 面板操作直到传输完成）

**简化策略（满足 v0.1 "传输时阻塞操作"）**：
- 触发传输后暂停接收用户键盘输入（除了 Ctrl-C）
- 用独立子 future 等待 channel 中的 progress，每次更新 redraw
- 完成后恢复正常

在 sftp_view 的底部渲染中，如有 `active_transfer`，替换 status_bar 区域为 `ProgressView`。

渲染调整 `sftp_view::render` 签名：

```rust
pub fn render(
    f: &mut Frame,
    host_alias: &str,
    pane: &mut SftpPaneState,
    transfer: Option<&TransferProgress>,
)
```

并在 transfer 存在时用 `ProgressView` 覆盖 status bar。

- [ ] **Step 4: 手测下载**

```bash
cargo run
```
连上 SFTP → 选中一个中等文件（几 MB）→ F5 → 进度条应该可见 → 下载完成提示 → 查看本地 `$PWD`/文件存在且内容正确。

然后本地选中一个文件 → F6 → 上传 → 远程 ls 验证。

- [ ] **Step 5: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src
git commit -m "feat(sftp): 单文件上传/下载 + 实时进度条"
```

---

## 阶段 5：收尾（T14–T15）

### Task 14: 双 Ctrl-C 返回 + 模式切换完善（T14）

**Files:**
- Modify: `src/app.rs`

**目标：** 按 REQUIREMENTS.md F6：
- SFTP 面板：500ms 内双 Ctrl-C → 断开连接返回主界面
- SSH 接管：Ctrl-C 透传给远程（不拦截）
- 主界面：单 Ctrl-C 清空搜索；搜索为空时退出
- 传输中：第一次 Ctrl-C 取消传输；第二次返回主界面
- 确认 Ctrl-Space 在 SSH→SFTP 和 SFTP→SSH 都生效

- [ ] **Step 1: 双 Ctrl-C 状态**

在 `App` 增加：

```rust
pub last_ctrl_c: Option<std::time::Instant>,
```

- [ ] **Step 2: SFTP 模式 Ctrl-C 逻辑**

在 `AppMode::Sftp` 的 `handle_key` 分支中增加：

```rust
(KeyCode::Char('c'), KeyModifiers::CONTROL) => {
    // 优先处理传输取消
    if let Some(t) = &self.active_transfer {
        t.cancel.cancel();
        self.last_ctrl_c = Some(std::time::Instant::now());
        return;
    }
    let now = std::time::Instant::now();
    let double = self.last_ctrl_c
        .map(|t| now.duration_since(t).as_millis() < 500)
        .unwrap_or(false);
    if double {
        self.last_ctrl_c = None;
        self.exit_sftp();
    } else {
        self.last_ctrl_c = Some(now);
    }
}
```

- [ ] **Step 3: 主界面 Ctrl-C 逻辑**

已在 Task 6 实现：搜索非空 → 清空；搜索空 → 退出。保持不变。

- [ ] **Step 4: SSH 模式 Ctrl-C 透传**

确认 `takeover` 里 stdin 读到 `0x03`（Ctrl-C）时没有拦截，直接发送。阅读现有代码 —— `takeover` 仅识别 `switch_byte = 0x00`，其他字节原样转发，Ctrl-C（0x03）自然透传。✓

- [ ] **Step 5: Ctrl-Space 双向切换**

从 SSH → SFTP：`takeover` 返回 `true` 时（前缀键触发）在 `ssh_takeover` 中：

```rust
match result? {
    true => self.enter_sftp_mode().await?,
    false => self.mode = AppMode::Main,
}
```

从 SFTP → SSH：SFTP 模式按 Ctrl-Space 触发重新进入接管：

```rust
AppMode::Sftp => match (k.code, k.modifiers) {
    (KeyCode::Char(' '), KeyModifiers::CONTROL) => self.trigger_ssh_resume = true,
    ...
}
```

`trigger_ssh_resume` 在 event_loop 中：退出 ratatui → 再次调用 `session.takeover(0x00)` → 结束后再返回 SFTP 或 Main。

- [ ] **Step 6: 手测**

完整走 F6 描述的 4 种 Ctrl-C 场景逐一验证；Ctrl-Space 来回切换至少 3 次，确认 SSH 会话在切换期间保持（远程 `ls /tmp`、切到 SFTP、切回后 shell 历史仍在）。

- [ ] **Step 7: clippy + fmt**

```bash
cargo clippy -- -D warnings
cargo fmt
```

- [ ] **Step 8: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): 完善各模式 Ctrl-C 语义与 Ctrl-Space 双向切换"
```

---

### Task 15: 端到端集成测试 + 冒烟验收（T15）

**Files:**
- Create: `tests/smoke.md`（手工冒烟脚本，checked-in）

**目标：** 整理一份可重复执行的冒烟脚本，覆盖 v0.1 核心交互；无自动化测试依赖（因涉及真实 SSH 服务）。

- [ ] **Step 1: 编写冒烟脚本**

创建 `tests/smoke.md`：

````markdown
# v0.1 冒烟验收脚本

> 需要至少 1 台可 SSH 的测试机，建议开个 Docker 的 openssh-server。

## 环境准备
- `~/.ssh/config` 中包含至少 1 个具名 Host（指向测试机）
- 本地 `$PWD` 下有一个大约 2-10 MB 的测试文件

## 步骤

1. **启动**：`cargo run`
   - ✅ 200ms 内出现主界面
   - ✅ 主机列表显示 ssh config 中的条目

2. **搜索**：输入主机名片段
   - ✅ 列表实时过滤
   - ✅ 清空搜索（Ctrl-C）或 Backspace 恢复

3. **SSH 连接**：选中主机 → Enter
   - ✅ 若使用 ssh-agent 则无交互直接登录
   - ✅ 若需密钥密码或登录密码，提示框正常弹出
   - ✅ 进入远程 shell，`ls` `top` 等命令正常运行

4. **resize**：拖动终端缩放
   - ✅ 远程 `top` 表格宽度同步变化

5. **切到 SFTP**：按 `Ctrl-Space`
   - ✅ 进入 SFTP 面板，显示远程 $HOME
   - ✅ 底部提示栏含 Tab/F5/F6/Ctrl-Space/Q

6. **目录导航**
   - ✅ Enter 进入子目录
   - ✅ 选中 `..` 回上级
   - ✅ Tab 切到本地视图，显示 `$PWD`

7. **下载**：远程选文件 → F5
   - ✅ 底部出现进度条
   - ✅ 完成后本地目录出现文件，大小一致

8. **上传**：本地选文件 → F6
   - ✅ 上传成功，远程可见

9. **取消传输**：F5 后立即 Ctrl-C
   - ✅ 传输取消，进度状态变化
   - ✅ 500ms 内再次 Ctrl-C → 返回主界面

10. **切回 SSH**：SFTP 面板按 `Ctrl-Space`
    - ✅ 回到 SSH 会话，历史命令仍在

11. **退出 SSH**：远程 shell 输入 `exit`
    - ✅ 返回 sushi 主界面

12. **退出程序**：主界面空搜索下 `Ctrl-C`
    - ✅ 终端恢复正常，无残留
````

- [ ] **Step 2: 执行冒烟脚本**

按上述顺序逐条跑通。任何失败项记录为 blocker，在本 task 内解决后再进 Step 3。

- [ ] **Step 3: 代码质量门禁**

```bash
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test --lib
cargo build --release
```
全部通过。

- [ ] **Step 4: 检查 release 二进制体积**

```bash
ls -lh target/release/sushi
```
预期：< 10MB。若超出，检查是否遗漏了 `Cargo.toml` 中的 release profile 优化。

- [ ] **Step 5: 更新 ROADMAP**

在 `ROADMAP.md` v0.1 部分，全部条目改为 `[x]`（当前已是 `[x]` 的话跳过）。

- [ ] **Step 6: Commit**

```bash
git add tests/smoke.md ROADMAP.md
git commit -m "chore: v0.1 冒烟脚本 + 发布准备"
```

- [ ] **Step 7: 打 tag**

（待用户确认后执行）
```bash
git tag v0.1.0
# 等用户 OK 后再 push --tags
```

---

## 总结与注意事项

### 依赖新增汇总（合并后 `Cargo.toml` 应包含）

新增 dependencies：
```toml
async-trait = "0.1"
futures = "0.3"
tokio-util = "0.7"
```

新增 dev-dependencies：
```toml
[dev-dependencies]
tempfile = "3.10"
```

`crossterm` 启用 `event-stream` feature：
```toml
crossterm = { version = "0.29", features = ["event-stream"] }
```

### 未覆盖但 v0.1 可接受的缺失

- **ProxyJump**：v0.1 导入时不处理。`ssh2-config` 对其支持不完整，实现多级跳板涉及 russh 的链式连接，列入 v0.5。
- **TOFU（host key 验证）**：`ClientHandler::check_server_key` 目前一律接受；v0.3 引入 known_hosts。
- **文件夹递归传输**：已标注 v0.2。
- **断点续传**：已标注 v0.2。

### 执行注意

- **每个 Task 末尾都要 commit**，保持 git 历史清晰、可回退。
- **先 TDD 的任务**（T1、T2、T3、部分 T6）务必先写失败测试；其他任务因涉及外部依赖，改为"小单测 + 手测"组合。
- **任何一次 `cargo check` 报错先解决，不要推进到下一 Step**。
- **russh / russh-sftp 的 API 以 `cargo doc --open -p <crate>` 为权威**，本文档给出的代码可能需要根据最新稳定版微调。

### 交付里程碑

- **Phase 1 完成**（Task 3 后）：纯逻辑层通过全部单测，可独立发布为 library 预览
- **Phase 2 完成**（Task 6 后）：`cargo run` 已可见完整主界面 + 真实 ssh config 导入
- **Phase 3 完成**（Task 10 后）：SSH 连接可用，用户已能替代 `ssh` 命令日常使用
- **Phase 4 完成**（Task 13 后）：SFTP 文件传输可用，v0.1 功能闭环
- **Phase 5 完成**（Task 15 后）：正式 v0.1.0 可发布
